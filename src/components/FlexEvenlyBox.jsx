import { Box } from "@mui/material"
import { styled } from "@mui/system"

const FlexEvenlyBox = styled(Box)({
  display: "flex",
  justifyContent: "space-evenly",
  alignItems: "center",
  flexWrap: "no-wrap"
})

export default FlexEvenlyBox